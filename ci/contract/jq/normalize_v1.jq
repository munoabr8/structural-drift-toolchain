{
  schema: "ir/v1",
  args:   (.args   // []),
  env:    (.env    // {}),
  reads:  (.reads  // ""),
  writes: (.writes // []),
  tools:  (.tools  // []),
  exit:   (.exit   // {}),
  outputs:(.emits  // .outputs // [])
}

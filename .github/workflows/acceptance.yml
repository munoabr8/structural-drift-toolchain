name: accept
on: [push, pull_request]
jobs:
  accept:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y jq bats
      - run: bash run_acceptance.sh
      - run: bats test/acceptance
